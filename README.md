EMacs Temp Cleaner (emtc)
-------------------------

A simple script to remove emacs '.*~' temp files from the /home/[user] folder recursively

#### Usage
```bash
bash emtc
```

...or set it globally (assuming you have root permissions)
```bash
chmod +x emtc
sudo cp emtc /usr/bin/
```
